# Sessione 2026-08-06-01

## Lavoro svolto

Ripresa da `docs/sessions/2026-08-05-03.md`: PERF-SAL (priorità massima richiesta
dall'utente) — `search_all_logs` percepibilmente più lento dopo il fix di correttezza della
sessione precedente (commit `722b054`).

### Diagnosi (prima di ottimizzare, su richiesta dell'utente)

Verifica se la pipeline rispettava il principio di pre-selezione dei log per finestra
temporale prima di misurare. Tre risultati hanno ribaltato l'ipotesi iniziale:

1. **Il ramo Guidewire di `search_all_logs.sh` (directory flat, senza `BASE` uniforme) non
   leggeva 30 rotazioni — ne leggeva 5.** `grep -v "[0-9]\{10\}"` scartava ogni nome con 10
   cifre consecutive (l'epoch delle rotazioni). Non era la causa della lentezza: era un bug
   di correttezza peggiore — "cerca X ieri" non poteva mai vedere lo storico.
2. **La vera regressione era il `match()` del timestamp eseguito su ogni riga di ogni
   file**, indipendentemente dal pattern. Misurato: 0.19s su 0.27s totali (208k righe).
3. **`mtime` non è affidabile** (confermato dall'utente: i log sono copiati/sincronizzati),
   quindi qualsiasi strategia di pruning basata su `stat` era da escludere.

### Fix (4 aree, 3 commit)

**Fase 1 — `580b6d6`, perf**: `search_all_logs.awk` riscritto a due passate gawk
(`FNR==NR`): la prima testa solo il pattern, la seconda (costosa) gira solo se esiste un
candidato. Per i `.gz`, due `process substitution` indipendenti (lo stream non è
rigiocabile). Misurato: file senza match 0.27s → 0.04-0.06s in locale (~5-6×), confermato
6-9× su file di produzione reale in un confronto interlacciato che eliminava il rumore del
carico server. Con match reale i tempi restano comparabili alla vecchia versione, come
atteso.

**Fase 3-4 — `89ff7da`, correttezza + motore generalizzato**: nuovo
`select_log_files_grouped` in `lib/utils-logfiles.sh` — walk backward con arresto
anticipato (algoritmo proposto dall'utente): per ogni gruppo di nome logico, parte dal file
più recente e scende alle rotazioni più vecchie solo se la finestra richiesta non è ancora
coperta. Le rotazioni sono contigue nel tempo, quindi `ts_end` non va mai letto — è derivato
dal `ts_start` del file successivo. L'ordine di recenza viene dal **nome** (non da `mtime`):
rotazioni con epoch nel nome ordinate per quel valore, rotazioni numerate con ordinale
invertito (`.log.1` è più recente di `.log.2/.10/.11` — un `sort` lessicografico
sbaglierebbe). `select_log_files` (firma storica) diventa un wrapper sottile; i 3 chiamanti
esistenti (`dispatch.sh:32`, `dispatch.sh:86`, `search_all_logs.sh`) restano invariati.
Rimosso il post-filtro ridondante in `open_glob_logs` (il raggruppamento per nome logico è
ora dentro il motore). Corretto un bug latente (`select_log_files DIR ""` emetteva la
directory stessa).

**Fase 5, consolidamento**: eliminato `log_ts_from_line` (codice morto, nessun chiamante).
`resolve-logs.sh` non hardcoda più i tre `*_LOG_BASE` (li legge da `system.conf`, con guard
esplicito se assenti — stesso pattern di `SERVER_LOG_FORMAT` in `dispatch.sh`). Non aggiunta
`ts_from_line` a `utils-time.awk` come da bozza iniziale del piano: senza un chiamante reale
sarebbe stata altro codice morto (il motore è in bash, non awk).

**Fasi 6-8 — `06e9e2a`**: nuovo `lib/utils-log.sh` (log_debug/info/warn/error, mai su
stdout, configurabile via `BOT_LOG_LEVEL`/`BOT_LOG_FILE`). Feedback progressivo su stderr in
`search_all_logs.sh` durante selezione e ricerca (condizionato a `[[ -t 2 ]]`, prefisso `⋯ `
per non rompere le asserzioni dei test sulla prima colonna). Nuovo
`tests/test-utils-logfiles.sh` (14 casi: rotazioni con epoch, rotazione numerata con
trappola lessicografica, file singolo, file vuoto, file senza timestamp, `mtime` falsato).
`tests/run-tests.sh` ora collega anche `test-param-extract.sh` e `test-search-all-logs.sh`,
prima orfani (eseguiti solo a mano, senza rete di regressione automatica).

**CLAUDE.md**: nuova sezione "Principi di progettazione" — 5 principi richiesti
esplicitamente dall'utente (generalizzazione, centralizzazione, logging DEBUG configurabile,
feedback progressivo, pruning conservativo), salvati anche in memoria persistente.

### Verifica

Locale: 77/77 (74 intent + 14 utils-logfiles + 62 param-extract + 13 search-all-logs).
Deploy dry-run poi reale su `lxprworkerliq01` (`/product/lana-bot`). Live: suite 77/77;
query esatta del bug di ieri → rotazioni del giorno raggiunte correttamente; stessa query
senza filtro temporale → storia completa della giornata (6 log, 1756 occorrenze); confronto
di performance rigoroso (snapshot statico, misure interlacciate per eliminare il rumore del
carico server, load average ~7-8) confermato: 6-9× più veloce sui file senza match, tempi
comparabili con match reale.

### Secondo blocco — centralizzazione feedback (`f354ea4`)

L'utente ha chiesto se la stringa di progresso comparisse in tutti i tool. Verificato: **no**,
`_sal_progress` era una funzione locale a `search_all_logs.sh`, contro i principi 2 e 4
appena scritti. Spostata in `progress_show`/`progress_clear` (`lib/utils-log.sh`) e chiamata
da `select_log_files_grouped`, il motore condiviso — verificato che ora compaia anche in
`filter_errors`. Resta specifico di `search_all_logs` solo il messaggio della fase di
ricerca (è l'unico tool che itera su una lista di file con un pool). Aggiunto
`BOT_PROGRESS=off`. 5 test con TTY simulato (`script -qec`) su una superficie prima
strutturalmente invisibile: tutte le altre suite catturano sotto `$(...)`, dove stderr non è
un TTY e `progress_show` ritorna subito.

### Terzo blocco — PERF pool + log di performance (`188bc03`)

L'utente ha segnalato che con 322 file la fase di ricerca resta lenta, e ha chiesto un log
di performance per valutare offline dove ottimizzare. Entrambe le cose fatte.

**Bug nel pool di worker.** Usava `wait "${pids[0]}"`, che attende il worker più VECCHIO
invece del primo che finisce: head-of-line blocking. Con la distribuzione reale dei file (su
un nodo: 77 vuoti, 65 <100KB, 111 <5MB, 36 >5MB) uno slot restava bloccato dietro a un file
da 40MB mentre decine di file vuoti aspettavano. Misurato su 120 file reali, a parità di 4
worker: **104.2s → 39.7s (2.6×)**. `wait -n` richiede bash ≥ 4.3 (server 5.1).

**`SEARCH_PARALLEL_JOBS` resta 4, ora per misura.** Benchmark su tutti i livelli (nodo a 16
core): `jobs=4` 41.7s/load 7.9 · `8` 42.9s/16.9 · `12` 36.7s/19.2 · `16` 34.8s/23.7 · `24`
34.6s/24.4. Da 4 a 24 si guadagna il 17% triplicando il load average, su un nodo dove girano
JBoss e Guidewire: il carico è **I/O-bound, non CPU-bound** (se lo fosse, 12 worker su 16
core avrebbero dato ~3×). Numeri documentati in `system.conf` — la raccomandazione data
inizialmente all'utente ("alzare i worker") è stata corretta dai dati.

**Log di performance.** `log_query` (`chatbot.sh`) da 6 a 13 colonne TSV: durata totale,
fase selezione, fase ricerca, file selezionati, file con match, byte, worker. Le colonne di
fase arrivano dal tool via `BOT_PERF_FILE` (i tool girano in processi figli, le variabili non
risalgono). Riusa il query log esistente — retention e rotazione già gestite, nessun secondo
sistema. `log_query` spostata a DOPO l'esecuzione dei tool: solo lì la durata è nota.
Nuovo `perf-report.sh`: aggrega per tool (mediana/p95/max), scompone le fasi, elenca le query
più lente, mostra il tempo per MB a fasce di volume. 4 nuovi test sul contratto
`BOT_PERF_FILE` (23 totali in `test-search-all-logs.sh`).

**Scoperta dal primo utilizzo del report**: su query piccole il **76% del tempo non è né
selezione né ricerca** — è classificazione neurale e setup (~180-200ms fissi per query). Era
invisibile prima: si ottimizzava la fase di ricerca perché era l'unica misurabile. Su query
grandi è irrilevante, su query piccole è la voce dominante.

### Quarto blocco — ottimizzazione I/O guidata dai dati (`75b1dc7` + `gated=1`)

L'utente ha segnalato che con 322 file la fase di ricerca resta lenta e ha chiesto se si
potesse usare `zgrep` per i `.gz`. Entrambe le cose hanno portato a risultati concreti.

**`zgrep` valutato e scartato con misura.** È uno script `sh` che fa esattamente
`gzip -cd | grep` (sua riga 217): nessuno può cercare in un gzip senza decomprimerlo, il
formato non ha corrispondenza posizionale fra byte compressi e testo. Misurato **più lento**
di `gunzip -c | grep`: pattern assente 0.94-1.43s vs 0.88-1.06s, early exit 0.12-0.16s vs
0.02-0.05s (l'overhead di due processi shell domina il lavoro utile).

**`pigz` come decompressore condiviso (3-4×).** Indagando il costo reale dei `.gz` è emerso
che la decompressione è **~90% del tempo** (sola `gunzip` 0.40s su 0.46s totali). `pigz -dc`
sposta CRC e scrittura su thread separati dal decoder: 0.09-0.17s vs 0.32-0.38s su un file
2.1MB → 60MB. Non parallelizza la decodifica dello stream (sequenziale per costruzione del
formato), quindi `-p` alto non serve. `GZ_CAT` in `utils-log.sh` come unica fonte di verità,
usato da `open_log()` in `dispatch.sh` — quindi **tutti** i 14 tool — oltre che da
`search_all_logs.sh` e `utils-logfiles.sh`.

**Pre-gate letterale `grep -qiF`.** Prima dell'analisi `gawk`, verifica se il pattern esiste:
se no, salta tutto. Usa `-F` e non `-E` perché i dialetti ERE di gawk e grep divergono
(`a\.b`, `{brace`) e un gate con motore diverso scarterebbe match reali — falso negativo
silenzioso. Un guard rileva i metacaratteri ERE e **salta il gate**, tornando a `gawk` puro.

**`gated=1`: eliminata la passata duplicata (il guadagno singolo più grande, -23%).** Emerso
da una domanda dell'utente su cosa venisse effettivamente letto: un `.gz` con match veniva
decompresso **3 volte** (gate + pass1 + pass2), perché la prima passata `gawk` rispondeva a
una domanda già risolta dal `grep`. Con `-v gated=1` gawk riceve il file una volta sola.
Verificata l'**equivalenza** dei due percorsi sull'eredità del timestamp nello stack trace
(il fix del 2026-08-05): plain gated / plain 2-pass / gz gated / gz 2-pass danno tutti
`3|2026-08-05 16:30:00|2026-08-05 16:30:00`. 4 test permanenti — una divergenza esisterebbe
solo sul percorso ottimizzato e sarebbe invisibile senza il confronto diretto.

**Progressione misurata sulla query reale dell'utente** (~297 file, 1.9 GB, tutti i nodi
prod), tutte le run sullo stesso comando e visibili in `perf-report.sh`:

| versione | totale | selezione | ricerca |
|---|---|---|---|
| base | 144.3s | 20.0s | 121.6s |
| + pre-gate | 118.7s | 18.3s | 98.7s |
| + pigz | 107.5s | 14.0s | 91.6s |
| + gated=1 | **83.2s** | 14.2s | **67.0s** |

**Da 2m24s a 1m23s (-42%)**, sopra il 2.6× già dato da `wait -n`. Nota che anche la selezione
è migliorata (20.0s → 14.2s) pur leggendo solo 4KB per file: su ~300 file l'overhead di avvio
del decompressore conta, e la centralizzazione di `GZ_CAT` ha portato il guadagno anche dove
non era stato cercato.

## Stato finale

Modello neurale non toccato (nessun retrain). Sette commit (`580b6d6`, `89ff7da`, `06e9e2a`,
`f354ea4`, `188bc03`, `75b1dc7`, + `gated=1`) — i primi quattro pushati su `origin`, gli altri
da pushare. Deploy effettuato e verificato live dopo ogni blocco. Suite: 77 PASS aggregati
(32 casi in `test-search-all-logs.sh`, 19 in `test-utils-logfiles.sh`).

### Quinto blocco — logging attivo in produzione

Su richiesta dell'utente, `QUERY_LOG_DIR` ora ha default `<dir di chatbot.sh>/logs`
(applicato in `chatbot.sh`, non in `system.conf`, perché `SCRIPT_DIR` non è ancora noto
quando quel file viene sourciato). Un path relativo funziona in qualsiasi installazione senza
configurazione per-host. `logs/` era già escluso da `deploy.sh:76` — quindi i dati accumulati
non vengono sovrascritti dai deploy — aggiunto anche a `.gitignore`. `perf-report.sh` replica
la stessa catena di risoluzione (`system.conf` → `system.local.conf` → default), altrimenti
cercherebbe i dati dove il bot non li scrive.

**Problema di permessi trovato e risolto in produzione**: la directory è stata creata da
`root` (sessione di manutenzione) mentre l'installazione appartiene a `uga04128`, rendendo il
log non scrivibile per l'utente normale — il logging si sarebbe spento **in silenzio**.
`_init_query_log` ora mette `g+w` su directory e nuovi file, e se il file esiste ma non è
scrivibile **avvisa su stderr** e disattiva il logging per la sessione invece di accumulare
query non registrate credendo di raccogliere dati.

**Primo dato reale dal logging in produzione** (query piccole, nodo singolo, ultime 2 ore):
`slow_requests` 5342ms · `count_status` 4699ms · `gc_stats` 657ms · `filter_errors` 583ms.
Un fattore ~8 fra tool sullo stesso nodo e la stessa finestra: i due lenti leggono l'access
log di Undertow, molto più grande del `server.log`. Il pattern "il volume determina il tempo"
si conferma anche fuori da `search_all_logs`.

### Sesto blocco — estendibilità verificata + metriche di fase per tutti i tool

L'utente ha chiesto se il lavoro su `search_all_logs` fosse estendibile. Verificato con
misure, non per analogia:

| ottimizzazione | estendibile? | perché |
|---|---|---|
| motore di selezione (walk backward) | **già attivo** | `open_logs_for` lo usa: `count_status` seleziona **1 file su 44** |
| `pigz` (`GZ_CAT`) | **già attivo** | è in `open_log()`, quindi tutti i tool con rotazioni `.gz` |
| `wait -n` (pool) | **no** | solo `search_all_logs` ha un pool; gli altri passano tutti i file a un solo `gawk` |
| pre-gate `grep -qiF` | **no** | i tool di aggregazione devono contare *tutte* le righe: niente da scartare |
| metriche di fase | **fatto in questo blocco** | |

Distinzione strutturale: `search_all_logs` risponde a una domanda **sparsa** ("questo pattern
esiste?") e può scartare lavoro; `count_status`/`slow_requests` rispondono a una domanda
**densa** ("conta e aggrega tutto") dove ogni riga serve per definizione. Classi di problema
diverse: la prima si ottimizza evitando lavoro, la seconda solo abbassando il costo per riga.

**Tre ottimizzazioni del parsing testate e SCARTATE.** `count_status.awk` fa
`match($0, /" ([0-9]{3}) /, a)` per riga (2.75s su 173k righe). Testate 3 alternative:
`match`+`substr` 0.89s (3×), `index()` 0.44s (6×), `split` su quote 3.12s. **Tutte danno
conteggi divergenti** sui 200 (174382 riferimento / 174430 / 174443): l'access log **non** ha
campi a posizione fissa (`NF` varia 13-22, URL con spazi e User-Agent multi-parola), quindi
`index`/`substr` slittano su URL contenenti `" ` + 3 cifre. Su un tool il cui scopo è contare,
un errore dello 0.03% silenzioso è peggio di 2 secondi. **La lentezza della regex originale è
la sua correttezza.**

**Metriche di fase estese a tutti i 14 tool.** `dispatch_tool` è ora un wrapper che misura e
delega a `_dispatch_tool_run`: strumentazione in un punto comune invece di 13 copie nei rami
del `case`. `open_logs_for` gira in `$(...)`, quindi in una **subshell** — le sue metriche
passano da un file temporaneo, altrimenti la selezione risulterebbe sempre 0 per tutti i tool
tranne `search_all_logs` (che si strumenta da sé e non viene sovrascritto).

**Il dato che ribalta la conclusione delle fixture.** Su fixture locali l'overhead fisso
sembrava dominante (261ms, 84.7%); su **volumi reali di produzione** è marginale:

| fase | fixture locali | produzione (34 MB/query) |
|---|---|---|
| selezione | 34ms (11.3%) | 163ms (3.9%) |
| analisi awk | 12ms (4.1%) | **3498ms (82.6%)** |
| overhead fisso | 261ms (84.7%) | 574ms (13.6%) |

Non si contraddicono: l'overhead è **costante** (~550-680ms, indipendente dal volume),
l'analisi è **proporzionale**. Query piccole → conta l'overhead; query reali → conta
l'analisi. Ottimizzare l'overhead avrebbe migliorato le query che già rispondono in 300ms
lasciando intatte quelle da 8s. Caso peggiore misurato: `slow_requests` 8682ms totali di cui
**7967ms dentro AWK** su un solo file da 34 MB. Throughput analisi: **9.7 MB/s**.

### Settimo blocco — `correlate_gc_slow`, il tool più lento (segnalato dall'utente)

L'utente ha chiesto di includere `correlate_gc_slow` nell'analisi, indicandone la causa
percepita ("la fase di analisi della correlazione prima di stampare il resoconto finale").
Diagnosi confermata: **è il tool più lento di tutti** — 10659ms totali di cui **9918ms (93%)**
nell'analisi, su 71 MB da 2 file (è l'unico che legge due sorgenti insieme).

**Causa: costo algoritmico, non volume.** Il loop di correlazione scandiva TUTTE le pause GC
per OGNI richiesta lenta — O(richieste_lente × pause_GC). Su dati reali: 18665 × 543 ≈ **10
milioni di iterazioni**. Isolato alzando la soglia (così quasi nessuna riga entra nel loop):
il loop pesava ~8s su ~18s.

Sostituito con un **indice per secondo** (`gc_at[epoch]`): la correlazione cerca una pausa
entro ±`gc_margin_s`, quindi bastano (2·margin+1) lookup hash invece di 543 iterazioni — da
O(pause) a O(1) per richiesta.

**Misura** (5 round interlacciati su snapshot statico — il server ha varianza enorme, la
stessa operazione fra 5 e 20s, quindi round singoli erano inutilizzabili):

| | mediana | min | max |
|---|---|---|---|
| vecchia | 8.37s | 5.20s | 19.67s |
| nuova | **5.68s** | 3.96s | 12.52s |

**-32%**, vince 4 round su 5 (il round perso è quello in cui la vecchia ha registrato il suo
minimo assoluto). End-to-end in produzione: 10659→7066ms e 6653→4076ms.

**Correttezza provata** su snapshot di produzione: 7 combinazioni soglia/finestra + le 20
righe di dettaglio `CORRELATA`, output identico, 0 divergenze. Le divergenze osservate sui
file *live* erano l'artefatto dei log in scrittura attiva (`Pause GC: 750` vs `751`): tutti i
casi con finestra passata erano già identici, solo quelli che includevano l'ora corrente
divergevano.

**Codice morto rimosso**, emerso dalla modifica: `gc_ts[]`, `gc_dur[]` (servivano al loop
sostituito) e `gc_hit[]` — quest'ultimo scritto e mai letto **già prima** della modifica.
Elimina anche l'ultimo dubbio sull'equivalenza: era il solo punto dove l'indice poteva
attribuire una pausa diversa, e non influenzava alcun output.

**Nuovo `tests/test-correlate-gc-slow.sh` (10 casi).** Il tool non aveva test unitari, solo
`smoke-tools.sh` che richiede log reali. Copre la finestra ±2s ai bordi esatti, le soglie, due
pause nello stesso secondo, pause equidistanti, e il **verdetto finale** — un errore lì
cambierebbe la conclusione mostrata all'utente, non solo i tempi. Verificato non tautologico:
con `gc_margin_s` alterato 2 test falliscono. Collegato a `run-tests.sh` (**78 PASS**).

## Pendenti per la sessione successiva

- **PERF-AWK (priorità sui dati)**: dopo il fix di `correlate_gc_slow`, il peggiore resta
  `slow_requests` (7967ms su 34 MB, 92% in analisi), poi `count_status` (5508ms). A differenza
  di `correlate_gc_slow` **non hanno costo algoritmico** — solo parsing proporzionale al
  volume, dove le 3 varianti provate rompevano i conteggi. Vincolo appreso: ogni variante va
  validata sui conteggi contro l'implementazione attuale su snapshot statico.
- **Metodologia di misura**: il server ha varianza 4× sulla stessa operazione. Qualsiasi
  misura futura richiede snapshot statico + round interlacciati + mediana su ≥5 campioni.
  Round singoli hanno prodotto conclusioni invertite due volte in questa sessione.
- **PERF-NNET (declassato dai dati)**: l'overhead fisso è ~574ms costanti, il 13.6% sulle
  query reali (era il sospetto principale quando misurato solo su fixture). Vale la pena solo
  dopo PERF-AWK, e riguarda le query piccole.
- **SRCH-1** (`BACKLOG.md`): gap ricerca testuale in un log specifico nominato —
  invariato, non toccato in questa sessione.
- Lasciare accumulare i dati in `logs/` e rileggerli con `./perf-report.sh` fra qualche
  giorno di uso reale: mediane e p95 diventano significativi solo con volume.
- Standing task: continuare la validazione manuale utente dei tool rimanenti in produzione.
- Possibile estensione futura (non richiesta oggi): applicare `ts_from_line` centralizzato
  a `grep_named_log.awk`/`tail_log.awk` se emerge un chiamante reale in bash — oggi lasciato
  fuori per non introdurre codice morto.
