# Sessione 2026-07-29 — #01

## Stato iniziale
- Modello: 90,48,15 — accuracy 94.62% su 780 esempi
- Backlog P1, ARCH-2/3 aperti; T1/T2/T3 aperti; O/UI/C completi

---

## Lavoro svolto

### P1 — search_all_logs parallelizzato
- Riscritta la sezione `search_all_logs` in `dispatch.sh`: da gawk con ring buffer a `grep -c` nativo + pool di worker bash con `&` + tmpfile numerati
- Output: tabella con barre proporzionali colorate (verde/giallo/rosso), conteggio hit, timestamp prima occorrenza, dimensione file, suggerimento dettaglio
- `SEARCH_PARALLEL_JOBS` (default 4) configurabile in `system.conf` di ogni profilo
- Fix bug `grep -c || echo 0` → `|| true` (grep -c esce con codice 1 su no-match ma stampa già `0`)
- Deploy e smoke test su prod: 33 log analizzati, output corretto

### ARCH-2 — filter_app_errors migrato a utils-jboss.awk
- Rimosso parsing posizionale `$1 $2 $4`, ora usa `parse_server_log()` + `_logger` da utils-jboss.awk
- Bug fix sort: il vecchio codice era decrescente (più frequenti prima); dopo migrazione a utils-dedup (ascendente) l'output era invertito → fix con iterazione reverse `for (i=_dup_n; i>=1; i--)`

### ARCH-3 — grep_named_log e filter_app_errors migrati a utils-dedup.awk
- `grep_named_log.awk`: 6 array dedup inline + sort sostituiti con `dedup_add`/`dedup_sort`; stampa custom mantenuta (formato LEVEL+ts diverso da dedup_print)
- `filter_app_errors.awk`: 5 array `cause_*` + bubble sort sostituiti con `dedup_add`/`dedup_sort`

### ARCH-4 — Check topologia in train.sh (nuovo)
- Aggiunto check in `train.sh` pre-training: confronta `NUM_FEATURES` (vocab.sh) con `cols(layer1.txt)-1`
- Se divergono: errore esplicito con comando corretto `./setup.sh --profile <dir>`
- Motivazione: scoperto durante il ciclo T3 — il training ripartiva silenziosamente sul vecchio modello 90-input con dataset 97-feature, producendo un modello corrotto senza messaggi

### T1+T2 — Vocabolario (unigrams.txt)
- Aggiunti 7 unigram in `profiles/liquido/unigrams.txt` (NUM_FEATURES: 90 → 97):
  - Sezione GC: `pause|pausa\b` (peso 2), `collector`
  - Sezione Distribuzione: `statistich|statistic`, `ripartizion|suddivision|breakdown`
  - Sezione Discriminatori: `traffico|traffich`, `chiamata|chiamate`, `correlazion|correla\b`
- T2 già coperto dal vocabolario esistente (riga 50-51 unigrams.txt)

### T3 — Retrain
- Ciclo completo: `setup.sh` (reinit 97,48,15) → `build-dataset.sh` → `train.sh`
- Risultato: **accuracy 95.56%** (905/947), best_mse=0.005527, best_epoch=316
- Aggiunti 10 esempi training per query che fallivano post-T1:
  - 5 esempi `gc_stats` con `statistiche gc/collector`
  - 5 esempi `slow_requests` con `chiamate lente`
- Dataset finale: 956 esempi (2 zero-vector residui: "problemi nel log jboss", "anomalie nel log jboss")

### --dry-run in chatbot.sh
- Nuovo flag `--dry-run`: mostra ranking completo classificatore senza eseguire tool
- Aggiunto `lib/infer-dry.sh`: ranking ordinato per prob decrescente, 1° verde, 2° giallo, altri dim, separatore visivo alla soglia 25%
- Mostra feature attive / totali e soglia confidenza nell'header
- Skip resolve_session_logs in dry-run (non servono i log)

### Documentazione docs/user/
- Creata `docs/user/` con 4 file Markdown per presentazione a pubblico medio (bash/awk esperti, reti neurali non esperti):
  - `01-overview.md` — cos'è, stack, numeri
  - `02-architecture.md` — flusso query, struttura directory, multi-profilo
  - `03-nlp-and-training.md` — vettore feature, unigram/bigram, topologia, pipeline training
  - `04-tools-and-modules.md` — 15 tool, 4 utility AWK, formato JBoss, ricerca parallela

---

## Stato finale
- Modello: **97,48,15** — accuracy **95.56%** su 956 esempi
- Backlog: P1 ✅ ARCH-1/2/3/4 ✅ T1/T2/T3 ✅ — aperti solo T4 (bassa priorità)
- Deploy prod: dispatch.sh, filter_app_errors.awk, grep_named_log.awk, layer*.txt, unigrams.txt, train.sh, chatbot.sh (pendente)

## Pendente al prossimo avvio
- 2 zero-vector residui nel dataset: aggiungere vocab per `problemi|anomali` → `filter_errors`
- T4 (bassa priorità): margine C7↔C8 (service_times / correlate_gc_slow)

## Note deploy
- `unigrams.txt` va deployato **senza `--checksum`** (rsync --checksum non rileva le modifiche
  in certi casi su questo server). Usare `rsync -av` senza flag checksum per i file di vocabolario.
