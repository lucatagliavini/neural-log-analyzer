# Sessione 2026-08-24-01 — SRCH-5: provato il sedicesimo tool, cinque difetti

## Contesto di partenza

`SRCH-5` era il **primo punto concordato** per questa sessione: `search_all_logs` è
l'**unico dei 16 tool mai eseguito sui log veri**, escluso dallo sweep del 2026-08-21 per
durata. La voce di backlog conteneva una previsione esplicita — «la probabilità che il tool
con la superficie maggiore sia l'unico pulito è bassa» — e due trappole di metodo da non
ripetere (leggere il solo file corrente invece del gruppo di rotazioni; provare da una copia
in `/tmp`, che rompe il layout a cartelle sorelle).

Eseguito sul nodo 4 di `prod`: **353 file, 294 MB, 190 `.gz`, due app**.

## Che cosa funzionava, e conta come metà del risultato

Verificato, non assunto: politica cross-app con colonna APP (principio 6), lettura `.gz`
(39 dei 40 file con match sono compressi), tempo di risposta **8,2–17,2 s** su 49 file con
4 worker, estrazione del pattern quotato, marcatore `*` con nota per i timestamp solo-ora.

## Cinque difetti

Dettaglio completo in `BACKLOG.md`, sezione **SRCH-5**. Qui solo ciò che non si deduce
dal diff.

**Ogni difetto è stato misurato contro una verità di riferimento `grep` indipendente**,
calcolata prima di guardare l'output del tool. È il metodo che li ha trovati tutti.

### Il difetto grave, e la sua simmetria

Due log del nodo sono **JSON-lines** col timestamp dentro un campo. Nessuna grammatica li
riconosceva, quindi il filtro temporale non li valutava. Misurato: `cerca "MOVE_TO_QUEUE"`
con finestra oggi dava **61 occorrenze contro 4 corrette** — 15,25×, lo stesso ordine di
THR-1.

La cosa più istruttiva non è il numero ma che **la stessa grammatica mancante causava due
difetti opposti in due tool**: `search_all_logs` contava tutto (61 contro 4),
`grep_named_log` — che filtra col criterio inverso — **non trovava nulla** (0 righe contro 1,
misurato con una mutazione deliberata). Un falso positivo clamoroso e un falso negativo
silenzioso dalla stessa radice.

E la parità dei gemelli era rotta **in un solo verso**: su JF4U il gemello bash riconosceva
il timestamp e quello awk no. Il caso peggiore, perché la selezione dei file *sembrava*
corretta e quindi niente suggeriva un problema.

### Il difetto che decideva dove cercare

`cerca "chiamata al nodo 7" nel nodo 4`, con `--node 4` **anche** sulla riga di comando: il
bot ha cercato sul **nodo 07**. La stringa cercata decideva dove si cercava.

Avevo misurato due leak e **dedotto altri sette dal codice**. Su indicazione dell'utente
l'ambito è stato allargato a tutti gli estrattori, e i sette dedotti si sono rivelati
**tutti veri** — compreso il più insidioso, `cerca "errori di ieri"` che sposta la finestra
temporale. Se mi fossi fermato ai due misurati avrei chiuso due noni del difetto.

### Il difetto trovato da un conteggio che non tornava

Avevo aggiunto 5 asserzioni e il totale della suite non si muoveva. La prima spiegazione era
corretta — `run-tests.sh` conta i file esterni come un PASS ciascuno — **ed era anche quella
che nascondeva il difetto**: `tests/test-normalize-query.sh`, 302 righe e 56 asserzioni, non
era nella lista del runner. Un file sano, semplicemente dimenticato, che non ha mai protetto
nulla — ed è il test del file che stavo modificando.

### Il difetto dell'apostrofo, trovato verificando la parità

Il ramo che produce `<PATTERN>` usava `'[^']*'` senza delimitatori, e in italiano
l'apostrofo è graficamente la virgoletta singola:

    «errori nell'ultima ora dell'app»  →  «errori nell<PATTERN>app»

L'espressione temporale **spariva dal vettore di feature**. Misurato sul classificatore:
confidenza da **66,2% a 56,8%**, e `search_all_logs` compariva al **13,3%** perché
`<PATTERN>` *è* il segnale «c'è una stringa da cercare».

È sopravvissuto perché **zero delle 1171 query etichettate contiene un apostrofo**: il
dataset non rappresenta la forma più naturale dell'italiano, quindi nessun test poteva
inciamparvi. Da qui la voce aperta `APOSTR-1`.

## Quattro errori miei, tutti intercettati da una misura

Li elenco perché sono il contenuto riutilizzabile della sessione più dei fix.

1. **Ho quasi riportato un terzo difetto inesistente.** Sospettavo una disparità fra i
   gemelli su `console.log`: erano **due file diversi di due app diverse**, e su ciascuno i
   due gemelli concordavano. Il marcatore che avevo scritto per rilevare la divergenza non è
   mai scattato, e ho controllato perché invece di fidarmi.
2. **Ho quasi dichiarato necessario un retrain.** Una query del dataset sembrava contenere
   entità dentro le virgolette: era una riga di **commento** (`#`). Le query reali con
   virgolette sono 36 e nessuna è affetta.
3. **Il mio rimedio conteneva il difetto che correggeva.** La prima `quoted_spans_of`
   emetteva prima tutte le virgolette doppie e poi le singole, mentre le sentinelle stanno
   nella stringa in ordine **posizionale**: su `trova 'x' e "y" ora` il ripristino li
   avrebbe scambiati.
4. **Ho creduto a un'assenza prodotta dal mio `grep`.** In produzione le righe `console.log`
   non comparivano nell'output e ho sospettato di aver perso dati (una regressione di
   correttezza, principio 5). Era il mio `head -8` che le tagliava. Ho verificato prima di
   scrivere che c'era un problema.

E due FAIL di test che erano **le mie attese sbagliate, non il codice**:
- il file misto: una riga senza data **eredita** quello dell'ultima riga datata, quindi è
  filtrabile — «senza data» non è «non filtrabile»;
- `«app»` non è un alias applicativo (`APP_SHORT_ALIASES` ha solo `cc` e `cm`), quindi la
  query attraversa la normalizzazione invariata, ed era proprio il punto.

## Una regressione causata dal fix, intercettata dai test preesistenti

Il mascheramento indiscriminato ha rotto **SRCH-4**: un log di **sistema** citato
(`errori nel "server.log"`) è un log che l'utente sta *nominando*, non una stringa da
cercare. Avevo previsto la sottigliezza per una pipeline e l'avevo mancata per l'altra —
il principio 8 nella sua forma esatta. I due test che l'hanno intercettata esistevano già.

## Stato finale

- **186 PASS / 0 FAIL** su `liquido` **e** su `usnext`, `--parity` **1171/1171** su entrambi
- **Nessun retrain**: `queries.txt` rigenerato bit-identico **col backend Python** (cioè col
  codice modificato), pesi `bef9198b…` invariati, `input 119`, training deterministico
- Ogni test nuovo eseguito **prima** del fix per vederlo fallire. Il loop di parità dei
  gemelli validato con una **mutazione deliberata**: ramo rimosso da un solo lato → 5 FAIL
  mirati, fra cui **entrambe** le asserzioni di parità; file ripristinato bit-identico
- Diff: **+901 / −38** su 15 file più `lib/utils-quoted.sh` nuovo. Circa un terzo sono test

## Verifica in produzione, prima del commit

Deploy preceduto da `--dry-run`. Due controlli fatti *prima* di trasferire: i due file dati
che comparivano nella lista (`queries.txt`, `dataset.txt`) hanno md5 **identico** a quello in
produzione — erano trasferimenti a vuoto per solo mtime, riscritti da `build-dataset.sh` — e
i pesi non comparivano affatto. Nessuna modifica al modello ha raggiunto il server.

Verificato dal vivo: 9 md5 identici al locale, `input 119`, pesi `bef9198b…`.

**Compatibilità 3.9 misurata, non assunta**: `build-dataset.sh` sul server (Python
**3.9.25**, ppc64le) rigenera `queries.txt` bit-identico. È il vincolo aperto il 2026-08-21,
e questa è la prima modifica Python che lo mette alla prova.

| verifica | esito |
|----------|-------|
| `cerca "MOVE_TO_QUEUE"` | **7** occorrenze = verità `grep` **rimisurata adesso** (64 in tutte le rotazioni) |
| `cerca "cc-1786662052345-375622044"` | **zero** — la stringa è del 15 agosto, fuori finestra |
| `cerca "chiamata al nodo 7" nel nodo 4` | scope **nodo 04**, non 07 |
| `cerca "api-gateway timeout"` | nessun avviso «hai nominato il log "api"» |
| `cerca "NPE" negli ultimi 7 giorni` | finestra **2026-08-18 → 2026-08-24**, 152 file |
| `cerca "NPE"` (oggi) | cross-app e `.gz` invariati, 43 file invece di 49 |

Due dettagli che valgono più dei sei OK:

**Il conteggio atteso era cambiato.** La verità di riferimento della mattina era 4, ma alla
verifica erano passate due ore e il log era cresciuto: il valore corretto era **7**, e il
tool dava 7. Confrontare con la misura vecchia avrebbe prodotto un falso allarme — su un log
vivo la verità di riferimento va **rimisurata al momento del confronto**.

**Il marcatore nuovo è scattato su dati reali, non costruiti**: `! 18 occorrenze NON filtrate
per data` su `console.log`, che è un file **misto** — le righe ClaimCenter portano *sia* `*`
(solo-ora) *sia* `!` (non filtrabile), quelle ContactManager `!` con timestamp datati.
Stamattina quelle stesse occorrenze erano contate in silenzio come se fossero di oggi.

E i file letti scesi da **49 a 43**: il walk non divora più l'intera retention del log JSON.

## Pendenti per la sessione successiva

Fonte di verità: `BACKLOG.md`.

- **`APOSTR-1`** (Media, **nuova**): su 1171 query etichettate **zero** contengono un
  apostrofo, mentre in italiano l'elisione è la forma naturale. Non è un difetto di codice
  ma una lacuna di rappresentatività, ed è la ragione per cui il difetto dell'apostrofo è
  sopravvissuto senza che alcun test lo sfiorasse. Aggiungere esempi con elisione richiede
  `build-dataset.sh` + `train.sh`, quindi **un retrain e un checksum nuovo** — per questo
  non è stato fatto insieme al fix.
- **`SRCHQ-1`** (Promemoria, **nuova**): la regola sugli apici singoli è ora in un punto solo
  ma resta un'euristica — una citazione fra apici non può contenere un apostrofo. Limite
  dichiarato e coperto da test, con la via d'uscita (virgolette doppie) già nel messaggio
  d'aiuto. La voce esiste perché la causa sia scritta invece di essere ridiagnosticata.
- **`DEPLOYVER-1`** (Media) invariata: `deploy.sh` non è versionato ma contiene il sentinel
  di identità che protegge `rsync --delete`.
- **`TOKEN-1` è CHIUSA**: token rimosso dai due `.git/config`, permessi portati a `600` e
  **revocato dall'utente** su GitHub. Verificato dopo la revoca: `gh` autenticato con scope
  `repo`, `fetch` e `push --dry-run` exit 0 su **entrambi** i repository. Vedi sotto.
- **`FLEX-1`** e **`GCFMT-1`** invariate.
- **Vincolo confermato per misura**: la produzione gira **Python 3.9.25**. Il codice Python
  del progetto deve restare compatibile con 3.9 — verificato oggi su una modifica reale.
- Il test manuale dell'utente sui 16 tool: con SRCH-5 chiusa, **tutti e 16 sono stati
  eseguiti almeno una volta sui log veri**.

## Coda della sessione: il token GitHub (TOKEN-1)

Segnalato il 2026-08-20 e il 2026-08-21 senza essere risolto; affrontato oggi su richiesta
dell'utente. Dettaglio in `BACKLOG.md`, voce **TOKEN-1**. Le quattro cose che l'accertamento
ha cambiato rispetto a come il problema era stato descritto:

**1. L'allarme peggiore era un falso allarme.** `git grep` ha trovato `ghp_` in un file
**versionato** (`docs/sessions/2026-08-17-01.md:331`) — che con un `origin` su GitHub
avrebbe significato un segreto **pubblicato**. Era la prosa che descriveva il difetto, con
un'ellissi: **zero caratteri** dopo il prefisso. Agire sull'allarme avrebbe significato
riscrivere la storia git per nulla.

**2. Il token era ridondante, non necessario.** `gh auth setup-git` risultava **già
configurato** prima che lo eseguissi: git era già istruito a chiedere le credenziali a `gh`,
che le tiene in un file **600**. Ma un URL che contiene `user:token@` fa sì che git usi
quelle credenziali e **non consulti mai** il credential helper. Il token stava scavalcando un
meccanismo già presente e meglio protetto — quindi il fix è **togliere**, non aggiungere.

**3. Lo stesso token era condiviso con un secondo repository.** `neural-bash` — il
predecessore da cui questo è stato separato — aveva lo stesso segreto nell'URL (verificato
per hash, non per ispezione). La revoca che l'utente stava per fare **avrebbe rotto i push di
quel repo** senza spiegazione apparente. Bonificato anche quello.

**4. SSH è escluso dall'ambiente, non da una preferenza.** `github.com:22` va in timeout e
**anche `ssh.github.com:443`**: il firewall aziendale li blocca entrambi. Con SSH fuori
gioco il criterio diventa «quale segreto è protetto meglio», e `gh` vince perché il suo
`hosts.yml` è già `600` contro i `644` di `.git/config`.

**Ordine seguito, e perché:** ho provato la **scrittura** (`git push --dry-run`, exit 0) con
l'URL privo di credenziali **prima** di rimuovere il token — altrimenti avrei distrutto
l'unica copia del segreto senza aver dimostrato che l'alternativa funziona.

**Esito**: nessun repository nella home ha più credenziali in un URL; i due `.git/config`
sono passati da `644` (leggibili da **qualsiasi utente** della macchina, per 21 giorni) a
`600`; `fetch` e `push --dry-run` verificati su entrambi. Il token resta in 9 trascrizioni
`~/.claude/projects/*.jsonl` — lasciate come sono su scelta dell'utente, in una directory
`700`, e inerti una volta revocato il token.

**Token revocato dall'utente**, e verificato *dopo* la revoca: `gh` ancora autenticato con
scope `repo` intatto, `fetch` e `push --dry-run` **exit 0 su entrambi** i repository, nessuna
credenziale in URL in tutta la home al ricontrollo finale.

La distinzione che vale la pena portarsi dietro: **«rimuovere» e «revocare» sono due azioni
diverse e solo la seconda protegge.** La prima toglie il segreto dal disco, la seconda lo
rende inutile a chi lo ha già letto. Averle confuse è il motivo per cui una voce di sicurezza
può *sembrare* chiusa restando aperta — ed è plausibilmente il motivo per cui questa è
slittata per due sessioni.

Nota che evita un errore in futuro: la credenziale di `gh` è un token **OAuth** (`gho_`) che
vive sotto *Authorized OAuth Apps*, pagina diversa dai Personal access token — revocare un
PAT non può rompere `gh`.
