# Sessione 2026-08-20-01 — La prima fase della pipeline: tre harness, 9 difetti, SRCH-4

## Contesto di partenza

Pendenti dalla sessione precedente: `GCFMT-1` (non urgente) e `SRCH-4` (diagnosticata il
2026-08-19 ma non implementata, su richiesta esplicita dell'utente).

L'utente ha aperto la sessione con una valutazione di metodo, non con un task: *«mi sembra
che abbiamo molti problemi nella fase iniziale del bot — parsing della riga ed estrazione
parametri. Inferenza sembra funzionare bene. Tools: risolviamo in fretta, struttura più
solida. Ha senso concentrarci sul togliere i bug rimanenti creando una suite di test più
precisa per la prima parte?»*

## La valutazione: conclusione confermata, diagnosi corretta

Misurati i 31 commit `fix(` dal 1° agosto: i file più toccati sono `dispatch.sh` (13) e i
tool AWK (~32), contro `normalize-query.sh` (4) e `param-extract.sh` (3). **Per conteggio
grezzo i bug stanno nei tool.** Ma i tool sono stati corretti tanto perché sono
**osservabili** e perché hanno fixture vere; la prima fase ha meno fix perché i suoi difetti
sono **silenziosi** — producono una risposta ben formata alla domanda sbagliata.

Tre buchi strutturali nella suite, misurati e non supposti:

1. Level 1 asseriva **solo il nome del tool** — 89 query, zero asserzioni sui parametri.
2. **11 dei 19 parametri** di `param-extract.sh` non avevano una sola asserzione; i 427
   righe di `utils-time.sh`/`utils-time.awk` ne avevano 5, indirette.
3. Il REPL **ha stato, i test no** — dichiarato in `run-tests.sh:245`. Due bug reali
   (`e43d1c6`, `01c5f6f`) erano venuti da lì, non catturabili per costruzione.

Riformulata anche l'ipotesi «l'inferenza funziona bene»: SRCH-4 mostra il meccanismo vero —
`normalize-query.sh` degradava la stringa e la rete faceva la cosa migliore possibile con
essa. **Non un errore di classificazione, un errore di normalizzazione che si manifesta come
misrouting.** Questo rafforza l'ipotesi dell'utente spostando la colpa a monte.

Scelta dell'utente sull'ambito: tutti e tre gli harness, poi i bug emersi, retrain incluso.

## Lavoro svolto

Dettaglio completo in `BACKLOG.md`, sezione **FASE-1**. Sintesi:

**Tre harness nuovi** — `tests/test-utils-time.sh` (82 asserzioni), Level 1b in
`run-tests.sh` (85), `tests/test-repl-state.sh` (34). `_front_end()` fattorizzata come punto
unico che replica la sequenza di `chatbot.sh`, usato da Level 1 e 1b: due repliche
divergerebbero (principio 2, ed è il difetto di NLOG-4).

**9 difetti corretti**: T1/T2 (giorno e ora del giorno erano rami alternativi della stessa
cascata invece di dimensioni indipendenti — un errore di modellazione che generava quattro
sintomi), T3 (`ultim[aei]` senza la `o`), T4 (`tra le X e le Y` documentata e non
implementata), T5 (nessuna validazione dell'ora, `mktime` normalizzava a 4 giorni nel
futuro), D1 (range a cavallo di mezzanotte, regola indicata dall'utente), P1 (`STATUS_CODE`
rubava numeri con altro ruolo), P2 (`applicativ[oa]?` senza il plurale), D2 (`in_range(0)`
escludeva le righe non databili).

**SRCH-4** chiusa senza retrain: nome di log di sistema quotato resta letterale, quindi la
query diventa identica alla forma senza virgolette che già funzionava.

**C1/C2 con retrain**: `NUM_FEATURES` 113 → 114 per il nuovo placeholder `<IP>`, dataset
1262 → 1296 righe labeled. C2 non era un gap di copertura ma di **generalizzazione** — i 6
esempi `filter_ip` contenevano IP letterali, quindi il modello vedeva ottetti invece della
forma.

## Tre lezioni di metodo, tutte già nel progetto e tutte ripetute

1. **Un test che fallisce non implica un bug nel codice** (FORMAT-1). Quattro asserzioni mie
   erano sbagliate e sono state corrette invece di toccare la produzione, più tre errori di
   aritmetica della fixture in `test-repl-state.sh`.
2. **Centralizzare significa migrare i chiamanti** (principio 8). `access_ts_ok()`, creata da
   FORMAT-1 per dire «formato non riconosciuto», **non aveva un solo chiamante**.
3. **`[[ ]] && cmd` come ultimo comando di una subshell** fa abortire il chiamante sotto
   `set -e` — la trappola di HELP-1 (`3859d62`), ricalpestata scrivendo `_front_end()`.

E una nuova: **un harness può asserire l'etichetta invece dell'effetto.** Le asserzioni sulla
persistenza della finestra guardavano il banner (`ACTIVE_TIME_*`) e non il valore passato ai
tool (`TIME_*`). Con una mutazione deliberata che svuotava l'eredità, il banner restava
corretto e **tutte le asserzioni passavano**. È LOGSEL-1 D2 in forma di test.

## Stato finale

- **174 PASS / 0 FAIL** su `liquido` e su `usnext` (suite passata da 96 asserzioni verdi)
- `--parity` **1146/1146** su entrambi i profili, 114 feature
- `test-train-regression.sh`: checksum rigenerato, **3 run bit-identici**; `TOPOLOGY` e
  `--inputs` ora derivati da `NUM_FEATURES` invece di scritti tre volte
- Fail-before/pass-after su tutto: 17 FAIL prima dei fix, 0 dopo; `test-repl-state.sh`
  validato con **due mutazioni deliberate** su `chatbot.sh`, produzione ripristinata
  bit-identica (verificato con `git diff --quiet`)
- **Il modello di stato del REPL era già corretto**: 34/34 senza alcun fix. Risultato
  negativo, ora blindato invece di non verificabile.

## Pendenti per la sessione successiva

- **`TIME-D1b`** (`BACKLOG.md`, nuova — istruzioni operative complete lì): **non è un lavoro
  da fare, è un dato da leggere.** `in_range(epoch<=0)` ora include la riga; il caso "tutte le
  righe non databili" è dichiarato da `access_ts_format_warning()`, il caso "alcune" no — di
  proposito, un avviso per ogni riga malformata di un log da 200.000 righe sarebbe rumore
  costante. Quanto pesi quel caso è un numero che esiste solo in produzione: una query con
  `BOT_LOG_LEVEL=debug` e `grep utils-time`. Sotto l'1% → si chiude; decine di punti →
  rollback di una riga.
  Corretto in coda alla sessione, dopo una domanda dell'utente su questa voce: il canale
  richiedeva *anche* `BOT_LOG_FILE`, quindi `BOT_LOG_LEVEL=debug` da solo non scriveva nulla —
  e una misura assente si legge come «zero righe non databili». Ora il fallback è
  `/dev/stderr`, come `utils-log.sh` documenta per tutto il resto del logging. Terza istanza
  in questa sessione dello stesso schema: **uno strumento di verifica che tace quando è mal
  configurato è indistinguibile da uno che conferma** (le altre due: `access_ts_ok()` senza
  chiamanti, e le asserzioni sul banner invece che sull'effetto).
- **`GCFMT-1`** (Quando serve): invariata.
- **Commit, push e deploy non fatti** in questa sessione — da decidere con l'utente. Il
  deploy comporta pesi nuovi (`NUM_FEATURES` 114) su entrambi i profili, quindi va fatto
  insieme al modello, non solo con il codice.
- Il test manuale dell'utente sui 16 tool resta in corso: altri riscontri possono emergere.
