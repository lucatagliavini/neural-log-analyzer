# Sessione 2026-08-21-01 — Recupero di una sessione interrotta a metà, e un canale diagnostico rotto

## Contesto di partenza

La sessione del 2026-08-20 (pomeriggio, **non** documentata in `2026-08-20-01.md`, che copre la
sessione precedente) si è interrotta per esaurimento del budget API — `429 Budget has been
exceeded` — **nel mezzo di un intervento**, non fra due task. L'utente ha salvato la
trascrizione in `2026-08-20-160712-local-command-caveatcaveat-the-messages-below.txt` nella
radice del progetto e ha chiesto di recuperare e finire il lavoro.

**Il punto esatto dell'interruzione**, ricostruito dalla trascrizione: il modello era già stato
riaddestrato a 119 feature e le 96 query di validazione passavano, ma la suite aveva **un FAIL**
— `tests/test-train-regression.sh` — e l'ultima azione registrata è la lettura di quel file.
Il lavoro era quindi funzionalmente completo e **formalmente non verificato**: lo stato peggiore
in cui fermarsi, perché l'albero di lavoro conteneva un modello nuovo e una suite rossa.

## Che cosa era già fatto (verificato, non assunto)

Ricostruito da `git diff` sull'albero di lavoro, non dalla narrazione della trascrizione:

- `lib/infer-dry.sh` — `LC_ALL=C sort -grk1` applicato, con il commento esplicativo
- `profiles/{liquido,usnext}/domain.conf` + `README.md` — `TOOL_DESC[service_times]` corretta,
  tre tool mancanti aggiunti alla tabella del README
- `nlp/unigrams.txt` / `nlp/bigrams.txt` — ancoraggi `\b` e 4 feature nuove, **115 → 119**
- `nlp/dataset/queries_labeled.txt` — 19 esempi nuovi (1152 → 1171)
- `nlp/models/intent_classifier/` — reinizializzato e riaddestrato a 119 input

## Lavoro svolto oggi

Dettaglio completo in `BACKLOG.md`, sezione **VOCFIX-1**. Sintesi dei tre pezzi mancanti:

**1. La fixture che congelava un valore derivato.** `test-train-regression.sh` aveva
`NUM_FEATURES=115` letterale — e il commento immediatamente accanto spiegava che `TOPOLOGY` era
*derivata* da `NUM_FEATURES` «invece di riscritta a mano (principio 2)». La centralizzazione si
era fermata **un livello troppo in basso**: aveva eliminato la duplicazione fra `TOPOLOGY` e
`--inputs`, non quella fra il test e `nlp/tools.conf`. Corretto sourciando `nlp/tools.conf`, che
calcola già `NUM_FEATURES` e `MODEL_TOPOLOGY`, più una **guard sulle colonne del dataset**.
Checksum rigenerato: `7e6fc068…`, bit-identico su 3 run.

**2. Il quarto difetto, segnalato ieri e non corretto.** `lib/resolve-logs.sh` stampava
`echo '[ERROR] …' >&2` **come testo** su stderr, in tutte e 5 le uscite d'errore. La forma
eval-able non è sbagliata in sé — `normalize-query.sh` la usa correttamente — ma dipende dal
contratto del chiamante: `source <(script)` esegue stdout **anche dopo `exit 1`**, mentre
`res=$(script) || {` lo **scarta**. Corretto, con un commento che spiega perché i due file
devono divergere.

**3. Verifica completa**, che è ciò che mancava per poter dire "fatto": 175 PASS / 0 FAIL su
`liquido` **e** su `usnext`, `--parity` inclusa.

## Tre lezioni di metodo, e sono le stesse di ieri

1. **Un test che fallisce non implica un bug nel codice** — qui era la fixture, non la
   produzione. Terza volta in due giorni.
2. **La guard nuova è stata validata con una mutazione deliberata** (dataset troncato di 4
   colonne), poi il dataset ripristinato **bit-identico** (md5 confrontato). Senza la
   mutazione la guard sarebbe stata codice non eseguito che *sembra* protezione.
3. **Il sintomo può accusare il posto sbagliato.** `[FAIL] checksum atteso ≠ ottenuto` incolpa
   la riproducibilità di `neural-c` — l'unica cosa che quel test deve misurare — quando la
   causa è un dataset più vecchio del vocabolario. La guard nuova fallisce *prima* delle 200
   epoche e nomina il rimedio.

## La scoperta della sessione: GAPREP-1

Nel verificare l'affermazione «`vocab-linter`: 0 vettori tutto-zero» — scritta nella voce di
BACKLOG e **non ancora misurata da me** — il `gap-report` ha mostrato 16 classi con gap, fra cui
`database`, `messaging`, `jgroups`, `integr` sotto `grep_named_log`. Sono **nomi di log**, che
`normalize-query.sh` assorbe in `<LOGFILE>` e che LOGF-3 **vieta** nel vocabolario.

`vocab-gap.sh:75` fa `query = tolower($2)` sul labeled **grezzo**: misura su uno stadio della
pipeline diverso da quello che le feature vedono. È **il terzo caso in due giorni dello stesso
errore di misura** — ieri era contare `api.log` sul file grezzo ottenendo un numero 25 volte
troppo grande.

Misurato sui 43 token distinti che il report stampa: 6 coordinate, 17 stop word grammaticali,
e fra i restanti frammenti CamelCase (`daolavaparametri`) e parole a basso potere discriminante
escluse di proposito (`richieste`, presente in 4 classi). **≈86% di rumore strutturale.**

Il valore della scoperta non è il difetto ma la sua conseguenza: dà una spiegazione
**meccanica** al fatto che `fallimenti — 3 esempi` sia stato stampato da `train.sh` a ogni
addestramento e mai letto (FLEX-1b). Con l'86% di rumore, ignorare il report è il comportamento
**razionale** — il difetto non è di chi non lo legge, è del rapporto segnale/rumore. E il report
non ha alcuna nozione di **potere discriminante**, che è invece il criterio vero del
vocabolario.

**Aperta e non implementata** (priorità Media): cambia cosa il report dichiara, quindi è una
decisione da prendere con l'utente.

## Stato finale

- **175 PASS / 0 FAIL** su `liquido` e su `usnext`, `--parity` inclusa
- `--parity` **1171/1171** su entrambi i profili, **119 feature**
- **Topologia coerente**: `input 119` in `model.txt` = 109 unigrammi + 10 bigrammi
- `test-train-regression.sh`: checksum `7e6fc068…`, **3 run bit-identici**
- `build-dataset.sh` **idempotente**: rigenerato, checksum dei pesi invariato
- I 3 misrouting chiusi, confidenze identiche sui due profili: `count_status` **97.2%**,
  `slow_requests` **98.9%**, `show_help` **98.1%**
- Spot-check sui confini a rischio: `quante richieste al secondo` → `traffic_volume` 76% vs
  `richieste sopra i 3 secondi` → `slow_requests` 99.1% (il discriminante grammaticale
  singolare/plurale tiene); `quante chiamate 503 al minuto` attiva **entrambi** i tool sopra
  soglia, che è la risposta corretta a una domanda ambigua
- `gap-report`: 0 vettori tutto-zero (era 1)

## Coda della sessione: commit, push, deploy — tutti fatti e verificati

**2 commit su `master`**, convenzione del repo (codice, poi docs): `e71a225` (fix VOCFIX-1) e
`508d99d` (docs + GAPREP-1 aperta), più `1443057` che corregge la riga di questo stesso
session log su commit/push/deploy. **Pushati**. Nessun collegamento Jira — questo repo usa
`BACKLOG.md` come fonte di verità. *(La seconda parte della sessione — GAPREP-1
implementata — ha aggiunto altri 2 commit, vedi sotto.)*

**Deploy** su `root@lxprworkerlana01:/product/lana-bot/`, precedeuto da `--dry-run`. Il punto
critico era che con `NUM_FEATURES` 115 → 119 **codice e pesi devono partire insieme**: il
dry-run l'ha confermato (`unigrams.txt`, `bigrams.txt`, `model.txt`, `weights.txt`,
`infer-dry.sh`, `resolve-logs.sh` nello stesso trasferimento).

**Il dry-run ha anche intercettato un errore che stavo per fare**: avrebbe copiato in
`/product/lana-bot/` la trascrizione della chat salvata dall'utente (64 KB di conversazione)
perché stava nella radice del repo. Spostata **fuori dall'albero** in
`/home/uga04128/chat-2026-08-20-neural-log-analyzer.txt` — non cancellata. È la ragione per cui
il dry-run non è una formalità: protegge da ciò che è nella directory, non solo da ciò che si
intende trasferire.

**Verifica dal vivo**, non assunta dal deploy riuscito:

- `input 119` in `model.txt` = 109 unigrammi + 10 bigrammi
- md5 **identici al locale** su `weights.txt`, `lib/infer-dry.sh`, `lib/resolve-logs.sh`
- le tre query prima difettose, in produzione: `show_help` **98.1%**, `slow_requests` **98.9%**,
  `count_status` **97.2%** — cifra per cifra come in locale
- query reale end-to-end su un access log da 49 MB: **145.289 richieste** analizzate,
  distribuzione dei codici corretta (200: 99.0%, 500: 152, tasso errore 0.11%)

Un dettaglio di metodo su quest'ultima. La prima query reale — «quante chiamate 503 stamattina»
— ha risposto *«Nessuna richiesta trovata nel periodo selezionato»*, che è **indistinguibile da
un filtro che si disattiva** (P1/T3/D2 di FASE-1 erano esattamente questo). Verificata con una
query di controllo che *doveva* restituire dati: nell'elenco dei codici i 503 non compaiono
affatto — stamattina ci sono stati 500, non 503. **La risposta vuota era corretta**, e ora lo si
sa invece di presumerlo.

## Seconda parte della sessione: GAPREP-1 implementata

Su scelta dell'utente fra i pendenti. Dettaglio completo in `BACKLOG.md`, sezione
**GAPREP-1**; qui solo ciò che non si deduce dal codice.

**Il piano è cambiato tre volte durante l'implementazione, sempre per una misura.**

1. **La stima iniziale era sbagliata di 2,6×.** Avevo detto «da 43 righe a ~8»: il 43 era
   l'effetto del troncamento `--top 6` *per classe*, che nascondeva il resto. Il numero vero
   alle impostazioni di default era **113 righe in 16 blocchi**, e il risultato è **74
   candidati unici** — non 8. Il report non è diventato corto: è diventato *ordinato*, ed è
   l'ordinamento a renderlo leggibile, non il filtro.
2. **Il rimedio ha quasi reintrodotto il difetto.** Nella lista stopword avevo messo `dall`,
   `come` e `dove`, che misurate sono **candidati veri** (`dall'ip` → 31/32 in `filter_ip`;
   `loggati come info` → `filter_app_errors`; `dove compare` → 9/9 in `search_all_logs`).
   Nasconderle avrebbe reso invisibili tre segnali reali — esattamente il difetto che la voce
   corregge. Da lì la regola scritta nel file, che senza quell'errore non ci sarebbe.
3. **Due errori miei intercettati dalla verifica**: avevo ammesso le cifre fra i caratteri
   dei token (`1000`, `2026`, `8101`, `0473954` in cima alla lista) e avevo fatto rispettare
   `--top` al `--porcelain`, così `gap-report.sh` annunciava «6 candidati» invece di 74 —
   sbagliato **per difetto**, il modo peggiore, perché fa sembrare il problema più piccolo.

**Un falso allarme, di cui vale ricordare l'esito**: `train.sh` ha prodotto pesi diversi e
ho sospettato una non-idempotenza. Il codice era corretto — `train.sh:11-12` documenta che
con topologia invariata **continua** per altre epoche (`--additional-epochs`). Il modello è
stato ripristinato dal backup e verificato bit-identico. È la terza volta in questa
sessione che il sospetto cadeva sul posto sbagliato.

**Due difetti preesistenti trovati per strada**, entrambi in `gap-report.sh`: un `|| true`
dentro una command substitution faceva stampare «✓ nessun gap rilevante» per una misura mai
avvenuta (bastava un `unigrams.txt` cancellato), e un `exit 0` saltava tutto il report se
mancava il dataset numerico — anche la sezione che non ne ha bisogno.

**Copertura di test da zero a 25 asserzioni** (`tests/test-vocab-gap.sh`), validate con due
mutazioni deliberate che producono un solo FAIL mirato ciascuna. L'assenza di test su questi
due script è una delle ragioni per cui il report ha potuto restare inutilizzabile a lungo.

**Stato**: 176 PASS / 0 FAIL su entrambi i profili con `--parity`. **Nessun retrain**:
vocabolario, dataset e pesi non sono stati toccati, e il checksum `7e6fc068…` lo conferma.

## Terza parte: lo sweep di produzione, chiesto dall'utente

*«Hai provato a testare sul server di produzione? Hai rilevato altri bug?»* — La risposta
onesta era **no, non davvero**: avevo verificato tre query in `--dry-run`, una reale più una
di controllo, e il nuovo gap report. Eseguiti allora **15 tool su 16** sui log veri del nodo
4 (`search_all_logs` escluso per durata). Dettaglio in `BACKLOG.md`, voci **THR-1**,
**SVCGRAN-1**, **TRUNC-1**.

**Un difetto grave, corretto** (THR-1): la soglia di latenza espressa in **secondi** veniva
ignorata. Stessa domanda, due formulazioni: `sopra 5 secondi` → 14.473 risultati con soglia
1000 ms, `sopra 5000 ms` → 1.214 con soglia 5000. **12× di differenza secondo come la
domanda è posta.**

Tre cose lo rendono più interessante di un fix di regex:

1. **Il fatto era già nel file, dodici righe sopra.** La `sed` che disambigua `STATUS_CODE`
   elencava già `secondi?|sec\b|millisecond[oi]?` fra le unità di latenza — cioè
   `param-extract.sh` sapeva che «5 secondi» è una latenza *per non confonderla con uno
   status code*, e non lo sapeva quando doveva **leggerla**. Il fix estrae un elenco solo,
   usato da entrambi i punti.
2. **Era coperto da test, ma solo dove funzionava.** Le quattro asserzioni su
   `THRESHOLD_MS` erano tutte in millisecondi. Un parametro «coperto» non è un parametro
   coperto in tutte le sue forme, e questa è la lezione più riutilizzabile della giornata.
3. **L'avevo reso raggiungibile io il giorno prima.** VOCFIX-1 ha insegnato al modello a
   instradare «hanno superato i 10 secondi» su `slow_requests`; il tool non sapeva leggerne
   la soglia. Correggere il routing ha scoperto un difetto a valle che il misrouting
   mascherava — **per questo un fix di routing va chiuso con una prova end-to-end, non con
   un dry-run.**

**Un sospetto scartato, e vale quanto un fix**: `filter_errors` dichiarava «nessun errore nel
server log» mentre erano in corso 113 HTTP 500. È **corretto** — le 43 righe ERROR/WARN del
`server.log` sono tutte alle 08:18, fuori dalla finestra richiesta, e i 500 sono loggati come
INFO (la ragione d'essere di `filter_app_errors`, che infatti li trova). Verificarlo ha
evitato di "correggere" un difetto inesistente.

**Due difetti aperti e non corretti**, su indicazione dell'utente: `SVCGRAN-1`
(`service_times` restituisce **una riga per 214.594 chiamate** perché raggruppa sul primo
segmento del path, e su `liquido` tutto vive sotto `/essigSXCC/`; misurato: 1 gruppo a
profondità 1, 13 a profondità 2, 37 a profondità 3) e `TRUNC-1` (`grep_named_log` tronca a
100 caratteri senza marcatore, mentre `slow_requests` e `tail_log` dichiarano il
troncamento sullo stesso schermo).

**Stato**: 185 PASS / 0 FAIL su entrambi i profili con `--parity`. Fail-before verificato:
8 FAIL prima del fix, 0 dopo, `param-extract.sh` ripristinato bit-identico durante la prova.

## Quarta parte: SVCGRAN-1 e TRUNC-1, chiuse su richiesta dell'utente

Dettaglio in `BACKLOG.md`. Qui le tre cose che non si deducono dal diff.

**1. La misura sul secondo profilo ha trasformato un bug in una coordinata.** Prima di
scrivere codice ho misurato `usnext`: a profondità 1 dà **6 gruppi**, cioè la granularità
**giusta**. `liquido` a profondità 1 dà **1 gruppo** e ha bisogno di 3. Due deployment della
stessa tecnologia con strutture URL diverse: è la definizione di coordinata (principio 7), e
senza quella misura avrei "corretto" un default globale rompendo il profilo che funzionava.

**2. L'avviso conta più del parametro.** Il tool ora dice, quando trova un solo gruppo, che
la profondità non distingue nulla e che i numeri sono quelli dell'access log intero. Senza,
il difetto era invisibile per costruzione — ed è per questo che è rimasto tale finché non si
è guardato l'output sui log veri. La soglia è **un** gruppo e non "pochi": due gruppi possono
essere la verità di un deployment, uno solo non lo è mai.

**3. La passata di principio 8 ha triplicato l'ambito di TRUNC-1.** Il troncamento
silenzioso era stato notato in `grep_named_log`; cercando la stessa forma è emerso anche in
`utils-dedup.awk` — il printer **condiviso** — più `filter_app_errors` e `filter_errors`.
Correggere solo il primo avrebbe lasciato tre gap identici e più difficili da notare perché
«sembra già risolto». Da qui `ellipsize()` in `utils-colors.awk`, caricato da ogni
invocazione gawk.
E la distinzione che rende la correzione non meccanica: **tre `substr` non vanno toccati**,
perché sono chiavi di deduplicazione — là il taglio *è* il meccanismo che fa collassare
messaggi simili. Ognuno ha ora un commento che lo dice, così la prossima passata non li
"corregge".

**Due inciampi di fixture, entrambi istruttivi.** Aggiungere la riga lunga a
`prod1nssd-cc.log` ha rotto un'asserzione che conta le righe ERROR (2 → 3): spostata in un log
proprio, perché una fixture condivisa accoppia test indipendenti. E il test del guard
falliva perché non avevo rediretto `LOG_BASE_DIR` sull'albero di fixture — verificava un
errore diverso da quello che voleva misurare.

**Verificato in produzione**, che è dove il difetto era stato trovato: `service_times` su
`liquido` ora dà 37 servizi con dati azionabili (`rest/agencyservices` 8011 ms su 1 chiamata,
`ClaimCenter.do` 46.876 chiamate con p99 7305, `service/ccfeireceiverequest` con il MAX da
146.999 ms), nessun avviso di degenerazione, e `usnext` **invariato** — 6 gruppi a profondità
1, esattamente come misurato prima. TRUNC-1: 12 righe con il marcatore.

**E la verifica in produzione ha aperto SVCGRAN-2.** Con profondità 3 i REST si separano bene,
ma i **SOAP collassano in una riga** (`essigSXCC/ws/it`): vivono in
`/essigSXCC/ws/it/unipol/sx/webservice/<area>/<Servizio>/soap11`, dove le componenti 3-6 sono
un **package Java**. Misurato: **14 servizi SOAP distinti** invisibili come tali, incluso
l'endpoint più trafficato di tutto il log (`WsUtility`, 5.789 chiamate). Non è una
regressione — prima erano invisibili tutti, ora 37 su ~50 — ma è una degenerazione
**parziale**, e l'avviso non la cattura perché scatta solo a un gruppo. È la stessa decisione
di design di SVCGRAN-1, riaperta da un dato che quando l'ho presa non avevo: la conferma che
verificare in produzione non è una formalità di chiusura ma una fonte di informazione.

**Stato**: 185 PASS / 0 FAIL su entrambi i profili con `--parity`; checksum dei pesi
invariato (nessun retrain).

## Quinta parte: VENVGATE-1, e una domanda generale che ne è uscita

Dettaglio in `BACKLOG.md`. Le tre cose che contano:

**1. Il gate era scritto tre volte, e il terzo caso era il peggiore.** Non solo
`build-dataset.sh` prendeva il ramo bash in produzione (0,2 s contro ≥110 s): anche
`tests/test-normalize-parity.sh` pretendeva il `.venv` e faceva **`exit 1`**, quindi su una
macchina senza venv `--parity` riportava un **FAIL senza che nulla fosse rotto**, con un
messaggio che suggeriva `pip install -r requirements.txt` — inutile, perché non serve alcun
pacchetto di terze parti. **E il quarto criterio l'avevo aggiunto io lo stesso giorno** con
GAPREP-1: il difetto non era solo storico, si stava replicando.

**2. La verifica che rende sicuro togliere il gate** non è che il codice compili, è che il
dataset generato dal `python3` di **sistema** sia **bit-identico** a quello generato dal
venv. Se divergessero, la produzione userebbe un dataset diverso dal locale e il modello
sarebbe addestrato su input non riproducibili.

**E la verifica in produzione ha smentito una mia affermazione.** Avevo scritto che là
girasse **3.12.3** come in locale: non l'avevo verificato, avevo letto solo il *path*
dell'interprete. È **3.9.25** — due minor release di distanza, cioè proprio la differenza che
rende la domanda non ovvia. L'esito è **più forte** di quello che avevo dichiarato:
`build-dataset.sh` su 3.9.25 rigenera `queries.txt` con md5 **identico** a quello di 3.12.3, e
`gap-report.sh` dà gli stessi 74 candidati. Ma è vero **per misura**, non perché lo
presupponessi — ed è la **quinta** volta oggi che un'affermazione non verificata è stata
corretta dalla verifica.
Conseguenza operativa che prima non era scritta: il codice Python del progetto deve restare
compatibile con **3.9**, non con la versione della macchina di sviluppo.

**3. Correggere un `exit 1` ha aperto una domanda generale: come si riporta un test che non
può misurare?** Ora la suite ha **tre esiti** — `0` misurato, `2` non misurabile, altro
fallimento — e `run-tests.sh` stampa `SKIP` senza contarlo né fra i PASS né fra i FAIL. Su
una macchina senza `python3`: **184 PASS / 0 FAIL con 2 SKIP espliciti**, invece di sei FAIL
che accusano il codice per una mancanza d'ambiente.

**E un errore mio, dello stesso tipo che stavo correggendo.** La prima versione della
guardia in `test-vocab-gap.sh` usciva **0**, quindi veniva contata fra i PASS: **un verde per
un test non eseguito**, cioè il difetto di GAPREP-1 reintrodotto dal test che lo protegge.
Corretta in `exit 2`. Nella stessa passata è emerso che l'asserzione «senza python3» usava un
`PATH` svuotato, che aveva smesso di funzionare (il venv si trova per path **assoluto**) e
dava `exit 127` — verificava un guasto diverso da quello che credeva.

**Stato**: 186 PASS / 0 FAIL su entrambi i profili con `--parity`; checksum dei pesi
invariato; dataset intatto.

## Sesta parte: SVCGRAN-2, e la verifica spostata *prima* del commit

Dettaglio in `BACKLOG.md`. Tre cose di metodo, tutte richieste dall'utente.

**1. Le alternative sono state misurate prima di scegliere la forma**, non dopo. La strada
ovvia — alzare la profondità — è stata **esclusa dai dati**: profondità 8 dà **1337 gruppi**
perché sfascia i REST. Il difetto non era «troppo poco profondo» ma «un prefisso fisso
(`it/unipol/sx/webservice`, un package Java) consuma quattro componenti». Da qui le sequenze
trasparenti, che togliono esattamente quello.

**2. Una feature scartata su misura.** Volevo generalizzare l'avviso di degenerazione
(«questo gruppo nasconde troppa struttura»). Misurato: `ws/it` nasconde 14 path distinti,
ma `service/app` ne nasconde **938** e `resources/img` **262**, e sono legittimamente un
gruppo. La metrica non distingue un gruppo che cela servizi da uno che cela risorse, quindi
l'euristica avrebbe gridato al lupo a ogni query. **Un'opzione scartata su dati è più solida
di una implementata su intuizione.**

**3. La verifica in produzione spostata prima del commit** — su scelta dell'utente, e ha
subito prodotto valore: ha richiesto **due correzioni di metodo**, non di codice.
- La prima invocazione leggeva il **solo file corrente** dell'access log, che alle 15:05 era
  appena ruotato: **680 righe invece di 333.296**. Ne avevo concluso «nessun SOAP, 10 gruppi»
  — una conclusione completamente sbagliata su una misura sbagliata. `dispatch.sh` include le
  rotazioni via `select_log_files_grouped`, la mia invocazione ad hoc no.
- La seconda moriva silenziosamente con `exit 1`: una copia in `/tmp` rompe il layout a
  **cartelle sorelle**, e `infer.sh` non trovava `../neural-c`. Il sintomo (nessun tool
  attivato) accusava il classificatore.

Entrambe avrebbero prodotto decisioni sbagliate se le avessi prese per vere. Copie temporanee
rimosse dal server a verifica conclusa, albero deployato mai toccato.

**Esito misurato in produzione**: `ws/ivr/CliIdentify` **1595 ms**,
`ws/unitools/UploaderUnitools` **1869 ms**, `ws/verbatel/VerbatelAPI` 804 ms — dove prima
c'era una sola riga `ws/it` a 446 ms che li mediava tutti. Profondità 4 scelta su misura
contro la 3, perché a 3 i SOAP escono per *area* e l'area `ivr` a 936 ms non dice che dentro
`CliIdentify` sta a 1595.

**Stato**: 186 PASS / 0 FAIL su entrambi i profili con `--parity`; 9 asserzioni nuove, fra cui
quella che protegge `/essigSXCC/service/ccfeireceiverequest` — l'endpoint che un filtro
per-segmento distruggerebbe.

## Pendenti per la sessione successiva
- **`GAPREP-1` è CHIUSA** (implementata nella seconda parte della sessione, vedi sopra).
- **`SVCGRAN-1` e `TRUNC-1` sono CHIUSE** (quarta parte della sessione, vedi sopra).
- **`search_all_logs` è l'unico tool non ancora provato in produzione**, escluso dallo
  sweep per durata. Da coprire al prossimo giro: è anche quello con la superficie più
  ampia (ricorre su tutti i log del nodo, di tutte le app).
- **`VENVGATE-1` è CHIUSA** (quinta parte) e **`SVCGRAN-2` è CHIUSA** (sesta parte).
- **Resta aperta `DEPLOYVER-1`** (Media): `deploy.sh` non è versionato ma contiene il
  *sentinel di identità* che impedisce a `rsync --delete` di cancellare una dest sbagliata
  — cioè un'invariante che nessuno può rivedere e che si perde se il file viene ricreato.
  Direzione proposta: estrarre `HOST`/`DEST` in un `deploy.local.conf` (schema di
  `system.local.conf`) e versionare lo script. È l'unica voce con implicazioni operative.
- **Vincolo nuovo, da rispettare in tutto il codice Python**: la produzione gira **3.9.25**,
  non la versione della macchina di sviluppo. Niente `match`, niente `X | Y` negli
  annotamenti, niente novità 3.10+.
- ~~**Lock stantio in produzione**~~ — **FALSO ALLARME, corretto in coda alla sessione.**
  Avevo scritto che `.neural-c.lock` su `lxprworkerlana01` (0 byte, 18 agosto) fosse «il
  residuo di un training interrotto». **Sbagliato, e l'avevo affermato senza verificarlo.**
  Misurato poi: il lock lo crea **ogni inferenza** (`neural-c predict`) — una singola
  query in `--dry-run` lo rigenera — e viene **riusato senza aggiornare mtime**, quindi la
  data del 18 agosto indica la sua **prima creazione** su quella macchina, non l'ultimo
  uso. È un file del tutto normale, e non c'era nulla da rimuovere.
  Dalla verifica è però uscita una cosa vera: `deploy.sh` non lo escludeva, quindi
  **spediva stato di runtime**. `rsync` sostituisce via temp + rename, cioè crea un inode
  nuovo: un'inferenza in corso che tiene il `flock` sul vecchio inode lo mantiene mentre
  una nuova lo prende sul nuovo, e **due processi crederebbero di avere il lock**.
  Improbabile e innocuo per un `predict`, non per un `train`. Aggiunta l'esclusione —
  **ma solo in locale**: `deploy.sh` è gitignorato per progetto (`.gitignore:21`, contiene
  l'host dell'installazione) e non esiste un template versionato, quindi la correzione
  **non è condivisibile e si perde** se il file viene ricreato. Vedi `DEPLOYVER-1`.
- **Il token GitHub in chiaro** in `.git/config` — **ancora da revocare**. Segnalato il
  2026-08-20, confermato presente oggi (`git remote -v` lo mostra nell'URL). È l'unica voce con
  implicazioni di sicurezza.
- **`FLEX-1`** resta come promemoria; **`GCFMT-1`** invariata.
- La trascrizione salvata dall'utente è stata spostata in
  `/home/uga04128/chat-2026-08-20-neural-log-analyzer.txt`, **fuori dal repo** (vedi sopra: nella
  radice sarebbe finita in produzione). Non versionata e non cancellata — da decidere se
  archiviarla o rimuoverla, dato che questo session log ne conserva il contenuto utile.
- Il test manuale dell'utente sui 16 tool riprende da qui. Scorciatoie: `--dry-run` ora ha il
  ranking **corretto** (prima il vincitore poteva comparire al terzo posto), e
  `./lib/param-extract.sh "<query>"` stampa tutti e 19 i parametri estratti.
