# Sessione 2026-07-31-04

## Lavoro svolto

### ARCH-6 completo (parte 2) — tutti i default hardcoded rimossi
- `dispatch.sh`: rimosso `:-jboss` da `SERVER_LOG_FORMAT`; errore esplicito se assente
- `dispatch.sh`/`search_all_logs.sh`: rimossi `:-undertow_access_log` / `:-server` / `:-gc` — letti da `system.conf`
- `normalize-query.sh` sezione 4: rimossi literal `"ClaimCenter"` / `"ContactManager"`; sostituiti con `APP_SHORT_ALIASES` + `APP_ALIAS_REGEX` da `entities.conf`
- `entities.conf`: aggiunti `APP_SHORT_ALIASES` e `APP_ALIAS_REGEX`
- Test: 35/35 PASS

### Fix bug loop dispatch interrotto (set -e)
- `chatbot.sh`: `dispatch_tool "$tool" || true` — con `set -e` un tool che ritorna 1 terminava lo script, saltando i tool successivi

### Fix ACTIVE_NAMED_LOG stale su cambio app
- `chatbot.sh`: `ACTIVE_NAMED_LOG=""` azzerato esplicitamente quando `ACTIVE_APP` cambia — evita che "log cc" di ClaimCenter venga ereditato da una query successiva su ContactManager

### Fix INFO colorato in bianco
- `utils-dedup.awk`: `INFO` → `WHT` (`\033[97m`) — propaga a `filter_errors`, `filter_app_errors`, `grep_named_log` (via `dedup_print`)
- `grep_named_log.awk`: stesso fix nel loop di stampa proprio del tool

### Fix timestamp su righe senza data (stack trace)
- `search_all_logs.sh`: prima di estrarre `first_ts`/`last_ts`, pre-filtra `_matches` tenendo solo le righe con timestamp riconoscibile — evita che le righe di stack trace (`Caused by:`, `at ...`) restituiscano `-` per i log Guidewire

## Stato finale
- Tutti i test 35/35 PASS
- Produzione aggiornata

## Commit sessione
- `392ca37` — ARCH-6 completo (refactoring hardcoding)
- `69293cc` — 3 fix: loop dispatch, NAMED_LOG stale, INFO bianco
- `213cc19` — fix timestamp stack trace in search_all_logs
- `e30a57b` — fix INFO bianco in utils-dedup.awk

## Pendenti / Da discutere
- **DEDUP-1**: normalizzazione ID numerici nella chiave dedup (`WorkItemPeriodicWork_Ext:ID`) — riduce rumore ma nasconde quanti WorkItem distinti sono bloccati. Da valutare se implementare come opzione, soglia, o modalità summary. Non implementare prima di discussione.
